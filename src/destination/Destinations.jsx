import React from 'react';
import Nav from "../navBar/Nav";

const Destinations = () => {
    return (
        <React.Fragment>
            <Nav/>

        </React.Fragment>
    );
};

export default Destinations;
